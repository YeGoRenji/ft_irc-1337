- if we don't send a "\r\n" at the end of the command (e.g using nc) server kaybda yt9a7bn
- getIp() is still hardcoded to "localhost", implement a decent one already
- send all channel messages on client join (We wont do this I guess ?)

fach katbedel smiya diyal nick ta3 dak l user f limchat ga3 makat updayta dik sa3at ==> khasha t updayta !!

check if TOPIC responses are flipped ? (TOPIC #ch :topic && TOPIC #ch)